import { TextDirectiveDirective } from './text-directive.directive';

describe('TextDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new TextDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
